React front end Node / Express back end hooked up to a Mongo DB


Set-up steps:

Install Node from https://nodejs.org/en/
npm init
npm install express



Install mongo
npm install mongo
run mongod

In Mongo client
use cities-dev
db.cities.insert({name: "name", description : "description"})

node app.js



